<!DOCTYPE html>
<html>
  <head>
    <title>Investor Ledger</title>

    <style>
      html {
        font-family: "helvetica neue", helvetica, arial, freesans, "liberation sans", "numbus sans l", sans-serif;
      }

      h1 {
        font-size: 2em;
        font-weight: 300;
        margin: 1em 0;
        text-align: center;
      }

      h1 > .small {
        font-size: 0.5em;
        font-weight: 300;
        color: #999;
      }

      .content {
        width: 800px;
        margin: auto;
      }

      table {
        width: 100%;
        border-collapse: collapse;
      }

      th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }

      th {
        background-color: #f2f2f2;
      }
    </style>
  </head>

  <body>
    <h1>
      <%= "Your investing account ($#{ @transactions.last.balance } available)" %>
      <div class="small">  <%= "#{ @transactions.first.date.strftime('%d/%m/%Y')  } - #{ @transactions.last.date.strftime('%d/%m/%Y') }" %></div>
    </h1>

    <div class="content">
      <table>
        <thead>
          <tr>
            <th>Date</th>
            <th>Type</th>
            <th>Description</th>
            <th>Amount</th>
            <th>Balance</th>
          </tr>
        </thead>

        <tbody>
          <!-- Insert transaction data dynamically here -->
          <% @transactions.each do |transaction| %>
            <tr>
              <td><%= transaction.date %></td>
              <td><%= transaction.transaction_type %></td>
              <td><%= JSON.parse(transaction.source)["description"] %></td>
              <td><%= transaction.amount %></td>
              <td><%= transaction.balance %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </body>
</html>
